# Third-party packages used by this project, for a Python 3.12 container.
# Unpinned - pip resolves the latest version at build time.

# Pinned: the low-level mcp.server.Server decorator API (@server.list_tools()
# / @server.call_tool(), used in McpWeather.py) must match. An unpinned build
# picked up an incompatible version whose Server lacked list_tools; 1.28.x is
# the API this code is written against.
mcp==1.28.1
starlette
uvicorn
python-dotenv
requests
ollama
sounddevice
vosk
aiohttp
pymysql
clickhouse-connect
python-dateutil
matplotlib

# Speech synthesis (text_to_voice.py). snac is the vocoder that turns the
# model's audio tokens into samples; it pulls einops and huggingface-hub.
# The Dockerfile installs torch from PyTorch's CPU index before this file
# is processed, so the requirement below is already satisfied by the CPU
# build - installing this file on its own instead gets the default PyPI
# wheel, which carries ~2.5 GB of CUDA libraries this project never uses.
numpy
#For developer computer
#torch --index-url https://download.pytorch.org/whl/cu126
#For PRO computer
torch --index-url https://download.pytorch.org/whl/cu130
snac
